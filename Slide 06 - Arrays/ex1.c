void preenche(int arr[L][C], int l, int c) {
    for (int i = 0; i < l; i++){
        for(int j = 0; j < c) {
            printf("Elemento %dx%d", i+1, j+1);
            scanf(%d, &arr[i][j]);
        }
    }
}
